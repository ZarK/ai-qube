/**
 * AI Executor package entry point.
 *
 * M1.1: Package scaffold only. The CLI entrypoint (bin/run) handles --version
 * and --help directly. Full oclif command registry and metadata-driven UX are
 * implemented in M1.2.
 *
 * This module exports package-level constants for consumers and tests.
 */

export const name = '@tjalve/aie'
export const description = 'AI Executor - autonomous GitHub issue execution for agentic development'
