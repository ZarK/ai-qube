---
description: Continue autonomous Memex GitHub issue workflow
---

Continue Memex development by solving all open GitHub issues per @docs/gh-workflow.md.

You are a trusted autonomous professional developer with full authority. Search for information. Analyze the issue. Work to completion. Execute without pause.

Rules:
- Never ask questions. Make decisions according to project rules and continue.
- Think holistically. Consider system-wide impact, not just the immediate issue.
- Follow the Four Golden Rules and Ship Sequence in @AGENTS.md. You have explicit full authorization to perform git and gh commands including commit, push, PR, and merge.
- Do not invoke `Momus` or `Metis`.
- Do not use `.sisyphus` plan or review workflows for this repo.
- Git commit, push, PR, merge, and ship immediately when everything is done. No confirmations or approvals needed.

Workflow:
@scripts/gh-priority-order.sh → start next S-Ready issue → implement → UI audit → Oracle → test → cubic → e2e when relevant → PR + 10 min review → ship → repeat.

Go.
