# Ambiguous JavaScript Workspace

This fixture has JavaScript workspace tooling metadata and a nested package without a root package manifest, so it must not be classified as a proven JavaScript TypeScript workspace.
