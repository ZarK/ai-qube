# Ambiguous Python Workspace

This fixture has Python workspace tooling metadata and a nested package without a root pyproject manifest, so it must not be classified as a proven Python workspace monorepo.
