{
  "name": "@tjalve/aib",
  "version": "0.2.2",
  "description": "AI Bootstrap - agent-operated project discovery and planning CLI.",
  "keywords": [
    "cli",
    "agent",
    "bootstrap",
    "planning",
    "documentation"
  ],
  "author": "tjalve",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZarK/ai-qube.git",
    "directory": "products/aib"
  },
  "bugs": {
    "url": "https://github.com/ZarK/ai-qube/issues"
  },
  "homepage": "https://github.com/ZarK/ai-qube/tree/main/products/aib#readme",
  "packageManager": "pnpm@11.0.4",
  "engines": {
    "node": ">=24.0.0"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/",
    "provenance": true
  },
  "bin": {
    "aib": "bin/run"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "bin/",
    "dist/",
    "docs/",
    "README.md"
  ],
  "dependencies": {
    "@tjalve/aie": "workspace:*",
    "@tjalve/qube-cli": "0.2.0",
    "@tjalve/qube-core": "0.2.1"
  },
  "optionalDependencies": {
    "@tjalve/qube-adapter-gitlab": "workspace:*",
    "@tjalve/qube-adapter-jira": "workspace:*",
    "@tjalve/qube-adapter-linear": "workspace:*"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build:deps": "pnpm --dir ../.. --filter @tjalve/qube-core run build && pnpm --dir ../.. --filter @tjalve/qube-adapter-gitlab run build && pnpm --dir ../.. --filter @tjalve/qube-adapter-linear run build && pnpm --dir ../.. --filter @tjalve/qube-adapter-jira run build && pnpm --dir ../.. --filter @tjalve/aie run build",
    "build": "pnpm run build:deps && pnpm run clean && tsc -p tsconfig.build.json",
    "typecheck": "pnpm run build:deps && tsc -p tsconfig.json --noEmit",
    "lint": "pnpm run typecheck",
    "test": "pnpm run build && node --test \"test/*.test.mjs\"",
    "prepack": "node ../../scripts/resolve-publish-dependencies.mjs && node ../../scripts/check-publish-manifest.mjs && pnpm run build",
    "postpack": "node ../../scripts/restore-publish-dependencies.mjs",
    "pack:check": "pnpm run build && pnpm pack --dry-run --json && node ../../scripts/restore-publish-dependencies.mjs",
    "verify": "pnpm run lint && pnpm run test && pnpm run pack:check"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "typescript": "6.0.3"
  }
}
