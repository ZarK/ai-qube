# Branch Naming Rules

Format: `<type>/<N>-<slug>`

1. `<type>/` is the required branch prefix.
2. `N` is the GitHub issue number.
3. `Slug` is a short lowercase hyphenated summary from the issue title.

Example: `fix/827-gh-harness`
